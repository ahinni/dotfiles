# Handle the fact that this file will be used with multiple OSs
platform=`uname`
if [[ $platform == 'Linux' ]]; then
  alias a='ls -lrth --color'
elif [[ $platform == 'Darwin' ]]; then
  alias a='ls -lrthG'
fi

alias amend="git commit --amend"
alias ali='vim ~/.dotfiles/bash/aliases'
alias bake='bundle exec rake'
alias bpr='open https://github.com/lockerdome/dub3/pulls'
alias bru='brew update && brew outdated'
alias d='cd ~/.dotfiles'
alias ga='git add'
alias gad='git add .'
alias gc='git commit'
alias gca='git commit -a'
alias gcleanup='git cleanup && git branch --merged | grep -v master | xargs git branch -d && git branch -a'
alias gcaa='git commit -a --amend -C HEAD'
alias gcl='git clone'
alias gcm='git commit -m'
alias gco='git checkout'
alias gd='git diff'
alias gdc='git diff --cached'
alias get='sudo apt-get install'
alias gf='git fetch && git status'
alias gg='git lg'
alias gp='git push'
alias gpr='hub pull-request'
alias gr='git rebase'
alias grc='git rebase --continue'
alias gs='git status'
alias irb='irb --readline -r irb/completion'
alias l='ls'
alias ll='ls -la'
alias ls='ls -G'
alias no='vim ~/Dropbox/aaron/notes.txt'
alias master='git checkout master'
alias patch='git format-patch HEAD^ --stdout > patch.diff'
alias remore='!! | more'
alias rerake='!! && rake'
alias retag='ctags -R --exclude=.svn --exclude=.git --exclude=log *'
alias repush="gpr && gpush"
alias rc='rails console'
#alias remigrate='rake db:migrate && rake db:migrate:redo && rake db:schema:dump && rake db:test:prepare'
alias so='source ~/.dotfiles/bash/aliases'
alias todo='vim ~/Dropbox/aaron/todo.txt'
alias patch='git format-patch HEAD^ --stdout > patch.diff'
alias pr='hub pull-request'
alias remore='!! | more'
alias rerake='!! && rake'
alias retag='ctags -R --exclude=.svn --exclude=.git --exclude=log *'
alias repush="gpr && gpush"
alias rc='rails console'
#alias remigrate='rake db:migrate && rake db:migrate:redo && rake db:schema:dump && rake db:test:prepare'
alias se='grunt server'
alias so='source ~/.dotfiles/bash/aliases'
alias te='grunt test:all'
alias u='cd ..'
alias vack='vi ~/.ackrc'
alias vi='vim'
alias w='cd ~/work/lockerdome'
alias wa="grunt watch"
