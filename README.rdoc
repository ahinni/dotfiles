=== Aaron Hinni's dot files

These are config files to set up a system the way I like it.
Borrowed heavily from git://github.com/r00k/dotfiles and the rbates original.

== Installation

  git clone git://github.com/ahinni/dotfiles ~/.dotfiles
  cd ~/.dotfiles
  rake install
